apiVersion: v1
kind: Service
metadata:
  name: ai-issue-agent-service
  labels:
    app: ai-issue-agent
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: ai-issue-agent

